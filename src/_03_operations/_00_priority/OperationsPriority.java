package _03_operations._00_priority;

/**
 * Operations priorities
 */
public class OperationsPriority {
	/*
	 * left to right:
	 * [], .()(вызов метода)
	 * !, ~, ++, --, +(унарная), -(унарная), ()(приведение), new
	 * *, /, %
	 * +, -
	 * <<, >>, >>>
	 * <, <=, >, >=, instanceof
	 * ==, !=
	 * &
	 * ^
	 * |
	 * &&
	 * ||
	 * 
	 * right to teft:
	 * ?:
	 * =, +=, -=, *=, /=, %=, ^=, <<=, >>=, >>>=
	 */
}
